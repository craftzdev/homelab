# Internal registry trust is machine configuration, not Cloud-Init state.
# The certificate is public material; its matching private key is restored
# from macOS Keychain into a Kubernetes TLS Secret during bootstrap.
apiVersion: v1alpha1
kind: RegistryTLSConfig
name: ${registry_endpoint}
ca: |-
  ${indent(2, trimspace(registry_ca_pem))}
